version: '3.1'
services:
  db:
    image: mysql:8.0.28-oracle
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: eshop
      MYSQL_USER: root
      MYSQL_PASSWORD: admin
